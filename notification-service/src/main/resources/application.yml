spring:
  application:
    name: notification-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/notifydb
      #uri:mongodb://admin:secret@mongodb:27017/notifydb?authSource=admin
    #config:
    #import: configserver:http://localhost:8888
  cloud:
    config:
      enabled: false   # 👈 disables config server completely
server:
  port: 8052
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


